# ─────────────────────────────────────────────────────────────────
# Copy this file to .env.local and fill in your values.
# ─────────────────────────────────────────────────────────────────

# ── SERVER-ONLY (no NEXT_PUBLIC_ prefix = never sent to browser) ──
# Get from: Firebase Console → Project Settings → Service Accounts
#           → Generate New Private Key → download JSON
#
# FIREBASE_PROJECT_ID     → "project_id" in the JSON
# FIREBASE_CLIENT_EMAIL   → "client_email" in the JSON
# FIREBASE_PRIVATE_KEY    → "private_key" in the JSON (keep the quotes!)

FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIE...\n-----END PRIVATE KEY-----\n"

# ── PUBLIC (safe for browser, used for WhatsApp link) ─────────────
# Your WhatsApp number WITH country code, NO + or spaces
# Germany: 49170123456  |  Saudi: 966501234567  |  UAE: 971501234567
NEXT_PUBLIC_WHATSAPP_NUMBER=49170123456

# Your store name
NEXT_PUBLIC_STORE_NAME=GreenHarvest
